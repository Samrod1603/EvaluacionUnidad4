using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScriptStateMachinePlayer : MonoBehaviour
{
    /*PlayerMovement pMove;
    public int state;
    public int attackState;
    shoot scriptShoot;

    void Start()
    {
        pMove = GetComponent<PlayerMovement>();
        state = 1;
        attackState = 1;
        scriptShoot = GetComponent<shoot>();
    }

    void Update()
    {
        switch (state)
        {
            case 1:
                pMove.Movement();
                break;
            case 2:
                pMove.EnemyKnockback();
                break;
            case 3:
                pMove.StayStill();
                break;
            case 4:

                break;
            case 5:

                break;
            case 6:

            default:
                break;
        }

        switch (attackState)
        {
            case 1: // attack
                scriptShoot.canAttack = true;
                break;
            case 2: // Interact
                scriptShoot.canAttack = false;
                break;
            default:
                break;
        }
    }*/
}
